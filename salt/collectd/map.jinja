# vi: set ft=json.jinja :

{% set map = salt['grains.filter_by']({
  'Debian': {
    '/etc/collectd/collectd.conf': {
      'file': {
        'name':       '/etc/collectd/collectd.conf',
      },
    },
  },
  'RedHat': {
    '/etc/collectd/collectd.conf': {
      'file': {
        'name':       '/etc/collectd.conf',
      },
    },
  },
}, merge=salt['config.get']('collectd')) %}
