{
  "checks": {
    "check-http-salt-halite": {
      "command"     : "/etc/sensu/plugins/http/check-http.rb -h 'localhost' -P '443' -p '/'",
      "handlers"    : ["default"],
      "interval"    :   60,
      "standalone"  :   true
    }
  }
}
